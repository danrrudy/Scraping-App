[pytest]
qt_api = pyqt5
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra --strict-markers
markers =
    current_schema: exercises behavior tied to the current MID column schema
    integration: crosses more than one application component
    qt: requires PyQt5 and pytest-qt
